import React from 'react';
import { Image } from 'react-native';
import { nextDisabled } from '../../images/';

const NextDisabledButton = () => <Image source={nextDisabled} />;

export default NextDisabledButton;
